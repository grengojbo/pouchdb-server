"use strict";

const utils = require('../utils');

module.exports = (app) => {
  // 404 handler
  app.use((req, res) => {
    utils.sendJSON(res, 404, {
      error: "not_found",
      reason: "missing"
    });
  });
};
