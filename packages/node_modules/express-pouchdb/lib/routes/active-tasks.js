"use strict";

const utils = require('../utils');

module.exports = (app) => {
  // Active tasks (stub for now)
  app.get('/_active_tasks', (req, res) => {
    // TODO: implement
    utils.sendJSON(res, 200, []);
  });
};
