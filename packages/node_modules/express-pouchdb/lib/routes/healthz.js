"use strict";

module.exports = (app) => {
  // handle listening
  app.get('/healthz', (req, res) => {
    res.status(200);
    // res.setHeader('', '');
    res.type('text/plain').send('Ok');
  });
};
